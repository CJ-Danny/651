import { shallowMount } from '@vue/test-utils'
import maintainerInfo from '@/views/manager/peopleManagement/maintainerInfo.vue'

describe('maintainerInfo.vue', () => {
    it('renders serviceman management interface correctly', () => {
      expect(true).toBe(true)
    })
  
    it('opens new serviceman dialog when clicking New button', () => {
      expect(true).toBe(true)
    })
  })