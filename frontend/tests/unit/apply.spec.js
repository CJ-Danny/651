import { shallowMount } from '@vue/test-utils'
import apply from '@/views/manager/apply/apply.vue'

describe('apply.vue', () => {
    it('renders application table with correct formatting', () => {
      expect(true).toBe(true)
    })
  
    it('handles pagination changes', () => {
      expect(true).toBe(true)
    })
  
    it('opens confirmation dialog for approval', () => {
      expect(true).toBe(true)
    })
  })