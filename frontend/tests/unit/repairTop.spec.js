import { shallowMount } from '@vue/test-utils'
import repairTop from '@/views/manager/repairment/repairTop.vue'

describe('repairTop.vue', () => {
  it('displays maintenance statistics navigation', () => {
    expect(true).toBe(true)
  })

  it('passes dashboard configuration to child component', () => {
    expect(true).toBe(true)
  })
})